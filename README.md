# Snake Game

A classic Snake game for the browser built with HTML, CSS, and vanilla JavaScript.

## Quick Start
Build a deployable bundle:
```bash
mkdir -p bundle
cp -R index.html game.js styles.css bundle/
```

Serve locally:
```bash
python3 -m http.server 8000
```

Open `http://localhost:8000` in your browser.
